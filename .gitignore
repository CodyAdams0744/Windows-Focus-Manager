# PyInstaller — auto-generated by build.py, do not commit
WindowFocusManager.spec
build/
dist/

# Inno Setup — generated installer output
installer_output/

# Distributable zip (build.py output) — share via GitHub Releases, not the repo
release/

# Generated icon (rebuilt each time by build.py)
icon.ico

# Logs (runtime + build/test artifacts)
wfm.log
wfm.log.*
*.log

# Python cache
__pycache__/
*.pyc

# Superseded by README.md
ReadMe.txt

# User config — generated at runtime, not meant to be committed
config.json

# Claude Code local settings
.claude/

# Design handoff files (prototype reference) — kept local, not for the repo
handoff/
